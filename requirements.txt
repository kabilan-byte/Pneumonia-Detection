# ──────────────────────────────────────────────────────────────
# Pneumonia Detection — Requirements
# Install with:  pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────

# Deep Learning framework
# (TensorFlow + Keras — CNN model build பண்ண)
tensorflow>=2.12.0

# Image processing
# (OpenCV — X-ray images load, resize, preprocess பண்ண)
opencv-python>=4.8.0

# PIL (Pillow) — for Streamlit image display
Pillow>=10.0.0

# Scientific computing
numpy>=1.24.0

# Machine learning utilities
# (Confusion matrix, classification report, metrics)
scikit-learn>=1.3.0

# Data visualization
# (Training curves, confusion matrix plots save பண்ண)
matplotlib>=3.7.0
seaborn>=0.12.0

# Web UI (Streamlit application)
streamlit>=1.28.0

# Kaggle dataset download (optional — needed for download_data.py)
kaggle>=1.5.16

# Progress bars
tqdm>=4.66.0
